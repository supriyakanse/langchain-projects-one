version: '3.9'

services:
  langchain-python:
    build: .
    container_name: langchain-python
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8000:8000"
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    environment:
      - PYTHONUNBUFFERED=1
